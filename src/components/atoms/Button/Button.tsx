import React from 'react';
import { TouchableOpacity } from 'react-native';
import { TextField } from '@components/atoms';

const Button = () => {
  return (
    <TouchableOpacity>
      <TextField type={''} children={''} />
    </TouchableOpacity>
  );
};

export default Button;
